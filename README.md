# Tracework

An AI data workspace where answers come with executable SQL, exact source versions,
and evidence. Local FastAPI + DuckDB + SQLite + React; a separate worker executes
approved recipes. Implementation and verification are in progress.
